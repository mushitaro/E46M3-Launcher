<?xml version="1.0" encoding="utf-8"?>
<!--
  E46 console switch, ///M surface treatment.

  The pressed state sinks the face and adds a 1 dp hard line at the top inner
  edge. Android shape drawables cannot express a true inner shadow, but at this
  size a single dark line reads as a recess and costs nothing.

  Nothing here scales or translates: pressing a button must not move the layout.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/m_raised" />
                    <corners android:radius="10dp" />
                    <stroke android:width="1dp" android:color="@color/m_border" />
                </shape>
            </item>
            <!-- the sink -->
            <item android:top="1dp" android:height="1dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/m_slate_950" />
                </shape>
            </item>
        </layer-list>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/m_panel" />
            <corners android:radius="10dp" />
            <stroke android:width="1dp" android:color="@color/m_border" />
        </shape>
    </item>
</selector>
