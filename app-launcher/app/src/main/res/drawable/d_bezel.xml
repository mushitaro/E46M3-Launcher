<?xml version="1.0" encoding="utf-8"?>
<!-- `box-shadow: 0 0 0 10px #000` around the display window: a solid black
     surround that separates the glass from the carbon. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FF000000" />
    <corners android:radius="@dimen/d_bezel_r" />
</shape>
